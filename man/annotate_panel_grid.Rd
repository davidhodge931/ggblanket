% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate_panel_grid2.R
\name{annotate_panel_grid}
\alias{annotate_panel_grid}
\title{Annotated panel grid lines}
\usage{
annotate_panel_grid(
  ...,
  x_breaks = NULL,
  y_breaks = NULL,
  colour = NULL,
  linewidth = NULL,
  linetype = NULL,
  theme_elements = "transparent"
)
}
\arguments{
\item{...}{Provided to require argument naming, support trailing commas etc.}

\item{x_breaks}{A vector of x-axis breaks for vertical grid lines. If NULL, no vertical grid lines are drawn.}

\item{y_breaks}{A vector of y-axis breaks for horizontal grid lines. If NULL, no horizontal grid lines are drawn.}

\item{colour}{The colour of grid lines. Inherits from current theme panel.grid.major etc.}

\item{linewidth}{The linewidth of grid lines. Inherits from current theme panel.grid.major etc.}

\item{linetype}{The linetype of grid lines. Inherits from current theme panel.grid.major etc.}

\item{theme_elements}{What to do with theme panel grid elements. Either "transparent", "keep" or "blank". Defaults "transparent".}
}
\value{
A list of annotate layers and theme elements.
}
\description{
Replace panel grid lines with annotated segments.
It requires a \code{coord} of \code{ggplot2::coord_cartesian(clip = "off")}.
}
\examples{
library(ggplot2)
library(dplyr)
library(stringr)
library(palmerpenguins)

set_blanket(
  theme = theme_lighter(
    panel_heights = rep(unit(50, "mm"), 100),
    panel_widths = rep(unit(75, "mm"), 100),
  ),
)

penguins |>
  tidyr::drop_na(sex) |>
  mutate(across(sex, \(x) str_to_sentence(x))) |>
  gg_blanket(
    x = flipper_length_mm,
    y = body_mass_g,
    col = sex,
  ) +
  annotate_panel_grid(
    x_breaks = seq(190, 230, 15),
    y_breaks = seq(3000, 6500, 250),
  ) +
  geom_point()

}
