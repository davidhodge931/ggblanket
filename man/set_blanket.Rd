% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_blanket-DEV.R
\name{set_blanket}
\alias{set_blanket}
\title{Set the style}
\usage{
set_blanket(
  theme = theme_lighter(),
  col = "#357BA2FF",
  linewidth = 0.66,
  linetype = 1,
  size = 1.5,
  shape = 21,
  stroke = 0.5,
  bordered_colour_by = function(x) ifelse(is_theme_dark(), col_screen(x),
    col_multiply(x)),
  bordered_fill_by = NULL,
  bordered_linewidth = 0.25,
  col_palette_discrete = scales::pal_hue(),
  col_palette_continuous = viridis_by_theme(n = 256, begin = 0.05, end = 0.95, option =
    "G"),
  col_palette_ordinal = NULL,
  col_palette_na = "#A6A6A6FF",
  shape_palette_discrete = c(21, 24, 22, 23, 25),
  linetype_palette_discrete = 1:6,
  titles_case = snakecase::to_sentence_case,
  perspective_axis_line_blank = TRUE,
  perspective_axis_ticks_rm = TRUE,
  perspective_panel_grid_rm = TRUE,
  ...
)
}
\arguments{
\item{theme}{A ggplot2 theme (e.g. \code{\link[=theme_lighter]{theme_lighter()}} or \code{\link[=theme_darker]{theme_darker()}}).}

\item{col}{A default hex code for the colour and fill of most geoms. Defaults to \code{blue}.}

\item{linewidth}{A default linewidth for most geoms. Defaults to 0.66.}

\item{linetype}{A default linetype for most geoms. Defaults to 1 (solid).}

\item{size}{A default size for point geoms.}

\item{shape}{A default shape for point geoms. Must be an integer between 0 and 25.}

\item{stroke}{A default stroke for point geoms.}

\item{bordered_colour_by}{A function with input of \code{col}. Defaults to \verb{(x) col_multiply(x)}. Affects colour as well as colour palette.}

\item{bordered_fill_by}{A function with input of \code{col}. Defaults to NA. Affects fill as well as fill palette.}

\item{bordered_linewidth}{A function with input of \code{linewidth}. Defaults to \(x) x / 2.64.}

\item{col_palette_discrete}{For a discrete colour/fill scale, a character vector or a \verb{scales::pal_*} function.}

\item{col_palette_continuous}{For a continuous colour/fill scale, a character vector or a \verb{scales::pal_*} function.}

\item{col_palette_ordinal}{For a ordinal colour/fill scale, a \verb{scales::pal_*} function. If NULL, determined from \code{col_palette_continuous}.}

\item{col_palette_na}{A NA colour/fill value.}

\item{shape_palette_discrete}{For shape scales, a numeric vector of shape codes. Defaults to c(21, 24, 22, 23, 25).}

\item{linetype_palette_discrete}{For linetype scales, a character vector or a \verb{scales::pal_*} function. Defaults to 1:6.}

\item{titles_case}{A function to apply to unspecified/unlabelled titles in \verb{gg_*} functions.
Defaults to \code{snakecase::to_sentence_case}.}

\item{perspective_axis_line_blank}{Logical. Whether \verb{gg_*} functions should remove the relevant
axis line based on plot perspective. Defaults to TRUE.}

\item{perspective_axis_ticks_rm}{Logical. Whether \verb{gg_*} functions should remove the relevant
axis ticks based on plot perspective. Defaults to TRUE.}

\item{perspective_panel_grid_rm}{Logical. Whether \verb{gg_*} functions should remove the relevant
panel grid based on plot perspective. Defaults to TRUE.}

\item{...}{Additional arguments (not used).}
}
\value{
Invisibly returns NULL. Sets global styling options as a side effect.
}
\description{
Set a consistent style for ggplot2 visualizations by configuring:
theme, geom default colour/fill, linetypes, linewidths, sizes,
fonts, palettes, and other styling options. Most users will only need
\code{theme}, \code{col}, \code{col_palette_d} and , \code{col_palette_c} arguments.
}
\examples{
library(ggplot2)
library(ggblanket)

# Use col_palette_d for both colour and fill
set_blanket(
  col_palette_d = c("#E69F00", "#56B4E9", "#009E73")
)

}
\seealso{
\code{\link[=theme_lighter]{theme_lighter()}}, \code{\link[=theme_darker]{theme_darker()}} for theme options
\code{\link[=col_multiply]{col_multiply()}} for creating accent colours
}
