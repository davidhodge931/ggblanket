% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate-rect.R
\name{annotate_rect}
\alias{annotate_rect}
\title{Annotate panel rectangle}
\usage{
annotate_rect(
  ...,
  xmin = -Inf,
  xmax = Inf,
  ymin = -Inf,
  ymax = Inf,
  fill = NULL,
  alpha = NULL,
  colour = NULL,
  linewidth = NULL,
  linetype = NULL
)
}
\arguments{
\item{...}{Arguments passed to \code{ggplot2::annotate("rect", ....)} (if normalised coordinates not used). Require named arguments (and support trailing commas).}

\item{xmin}{A value of length 1. Defaults to -Inf. Use I() to specify normalized coordinates (0-1).}

\item{xmax}{A value of length 1. Defaults to Inf. Use I() to specify normalized coordinates (0-1).}

\item{ymin}{A value of length 1. Defaults to -Inf. Use I() to specify normalized coordinates (0-1).}

\item{ymax}{A value of length 1. Defaults to Inf. Use I() to specify normalized coordinates (0-1).}

\item{fill}{The fill colour of the rectangle. Inherits from the current theme panel.background fill.}

\item{alpha}{The transparency of the rectangle. Defaults to 1 (fully opaque).}

\item{colour}{The border colour of the rectangle. Inherits from the current theme panel.background fill.}

\item{linewidth}{The border linewidth of the rectangle. Inherits from the current theme panel.border linewidth. Supports rel() for relative sizing.}

\item{linetype}{The border linetype of the rectangle. Defaults to 1.}
}
\value{
A list containing an annotation layer.
}
\description{
Create an annotated rectangle in the panel background.

This function is designed to work with a theme that is globally set with \link{set_blanket} or \link[ggplot2:get_theme]{ggplot2::set_theme}.
}
\examples{
library(ggplot2)

set_blanket()

p <- palmerpenguins::penguins |>
  gg_blanket(
    x = flipper_length_mm,
    y = body_mass_g,
    col = species,
  )

# Using data coordinates with theme defaults
p +
  annotate_rect(
    xmin = 225,
    fill = "#8991A1FF",
    alpha = 0.2,
  ) +
  geom_point()

# Using normalized coordinates
p +
  annotate_rect(
    xmin = I(0.9),
    fill = "#8991A1FF",
    alpha = 0.2,
  ) +
  geom_point()

}
