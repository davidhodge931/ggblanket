% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate.R
\name{annotate_panel_background}
\alias{annotate_panel_background}
\title{Annotate panel background}
\usage{
annotate_panel_background(
  xmin = NULL,
  xmax = NULL,
  ymin = NULL,
  ymax = NULL,
  fill = NULL,
  colour = "transparent",
  theme = "keep",
  ...
)
}
\arguments{
\item{xmin, xmax}{The horizontal boundaries of the panel background area.
Defaults to \code{-Inf} and \code{Inf} respectively.}

\item{ymin, ymax}{The vertical boundaries of the panel background area.
Defaults to \code{-Inf} and \code{Inf} respectively.}

\item{fill}{The fill colour of the rectangle. Defaults to the panel background fill from the current theme.}

\item{colour}{The border colour of the rectangle. Defaults to \code{"transparent"}.}

\item{theme}{How to handle existing panel elements. Either \code{"keep"} (default, overlay only),
\code{"transparent"} (make panel background transparent), or \code{"blank"} (remove panel background).}

\item{...}{Additional arguments passed to \code{annotate("rect", ...)}.}
}
\value{
A list containing annotation layers and optionally theme modifications.
}
\description{
Create a custom panel background area by drawing a rectangle with the panel
background fill ("keep" mode) or by removing/modifying the panel background and redrawing it in a
specified area ("transparent" or "blank" modes).

This function is designed to work with a theme that is globally set with \link{set_blanket}
or \link[ggplot2:get_theme]{ggplot2::set_theme}.

When \code{theme = "keep"} (default), the function draws a rectangle with the panel background fill
in the specified area. This is simple and works well when you want to add panel background
to a specific region.

When \code{theme = "transparent"}, the function makes the panel background transparent and removes
the border, then redraws the panel background only in the specified area.

When \code{theme = "blank"}, the function removes the panel background and border, then redraws
the panel background only in the specified area.

Note that \code{"transparent"} and \code{"blank"} produce visually similar results but are provided
for consistency with other annotate functions.
}
\examples{
library(ggplot2)

set_blanket()

p <- palmerpenguins::penguins |>
  gg_blanket(
    x = flipper_length_mm,
    y = body_mass_g,
    col = species,
  )

# "keep" mode: Simple overlay with panel background (default)
p +
  annotate_panel_background(xmax = 230)

# "transparent" mode: Make panel transparent, then redraw in specified area
p +
  annotate_panel_background(xmax = 230, theme = "transparent")

# "blank" mode: Remove panel background, then redraw in specified area
p +
  annotate_panel_background(xmax = 230, theme = "blank")

# Create a panel window with custom fill
p +
  annotate_panel_background(
    xmin = 180,
    xmax = 220,
    ymin = 3500,
    ymax = 5500,
    fill = "lightblue",
    colour = "blue"
  )

}
