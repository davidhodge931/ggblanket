% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate_panel_background.R
\name{annotate_panel_background}
\alias{annotate_panel_background}
\title{Annotated panel background rectangles}
\usage{
annotate_panel_background(
  ...,
  x = c(-Inf, Inf),
  y = c(-Inf, Inf),
  fill = "#8991A1FF",
  alpha = 0.25,
  colour = "transparent",
  linewidth = 0,
  linetype = 1
)
}
\arguments{
\item{...}{Require named arguments (and support trailing commas).}

\item{x}{A vector of length 2. Defaults to c(-Inf, Inf).}

\item{y}{A vector of length 2. Defaults to c(-Inf, Inf).}

\item{fill}{The fill colour of the rectangle. Defaults to "#8991A1FF".}

\item{alpha}{The transparency of the rectangle. Defaults to 0.25.}

\item{colour}{The border colour of the rectangle. Defaults to "transparent".}

\item{linewidth}{The border linewidth of the rectangle. Defaults to 0.}

\item{linetype}{The border linetype of the rectangle. Defaults to 1.}
}
\value{
A list containing an annotation layer.
}
\description{
Add background rectangles to highlight specific regions of a plot.
Useful for indicating periods of uncertainty, different phases, or other regions of interest.
It requires a \code{coord} of \code{ggplot2::coord_cartesian(clip = "off")}.
}
\examples{
library(ggplot2)

palmerpenguins::penguins |>
  gg_blanket(
    x = flipper_length_mm,
    y = body_mass_g,
    col = species,
  ) +
  annotate_panel_background(
    x = c(225, Inf),
  ) +
  geom_point()

}
