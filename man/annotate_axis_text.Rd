% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate.R
\name{annotate_axis_text}
\alias{annotate_axis_text}
\title{Annotate axis text}
\usage{
annotate_axis_text(
  ...,
  position = NULL,
  x = NULL,
  y = NULL,
  label = NULL,
  colour = NULL,
  size = NULL,
  family = NULL,
  length = NULL,
  hjust = NULL,
  vjust = NULL,
  angle = 0,
  theme = "keep"
)
}
\arguments{
\item{...}{Require named arguments (and support trailing commas).}

\item{position}{The position of the axis text. One of \code{"top"}, \code{"bottom"}, \code{"left"}, or \code{"right"}. Ignored if both \code{x} and \code{y} are provided.}

\item{x}{A vector of x-axis breaks for text positioning. Use \code{I()} to specify normalized coordinates (0-1).}

\item{y}{A vector of y-axis breaks for text positioning. Use \code{I()} to specify normalized coordinates (0-1).}

\item{label}{A vector of text labels or a function that takes breaks and returns labels. If \code{NULL}, uses appropriate formatting based on data type.}

\item{colour}{The colour of the text. Inherits from the current theme \code{axis.text} etc.}

\item{size}{The size of the text. Inherits from the current theme \code{axis.text} etc.}

\item{family}{The font family of the text. Inherits from the current theme \code{axis.text} etc.}

\item{length}{The tick length as a grid unit. Use \code{rel()} to scale relative to default length. Negative values or \code{rel()} with negative multiplier place text on the opposite side of the axis (inside the panel). Inherits from the current theme \code{axis.ticks.length} etc.}

\item{hjust, vjust}{Horizontal and vertical justification. Auto-calculated based on position if \code{NULL}. When \code{length} is negative, justification automatically adjusts for the flipped position.}

\item{angle}{Text rotation angle. Defaults to \code{0}.}

\item{theme}{What to do with the equivalent theme elements. Either \code{"keep"}, \code{"transparent"}, or \code{"blank"}. Defaults to \code{"keep"}.}
}
\value{
A list of annotation annotates and theme elements.
}
\description{
Create annotated text labels for axis breaks.

This function is designed to work with a theme that is globally set with \link{set_blanket} or \link[ggplot2:get_theme]{ggplot2::set_theme}.

It should be used with a \code{coord} of \code{ggplot2::coord_cartesian(clip = "off")}.
}
