% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/refine.R
\name{modern_drift}
\alias{modern_drift}
\title{Modern drift refine}
\usage{
modern_drift(..., discrete = "none", orientation = NULL)
}
\arguments{
\item{...}{Reserved for future extensions. Placed first so later arguments
must be named, and to support trailing commas in calls.}

\item{discrete}{Character scalar describing which axes should be treated as
discrete for refinement purposes: \code{"none"}, \code{"x"}, \code{"y"}, or \code{"both"}.}

\item{orientation}{Character. The primary axis of interest: \code{"x"} or \code{"y"}.
This affects grid modes such as \verb{*_drift()} and \verb{*_flow()}. If \code{NULL}
(default), orientation is inferred from \code{discrete}: \code{"y"} gives \code{"y"},
otherwise \code{"x"}.}
}
\value{
A ggplot2 theme object
}
\description{
Removes axis lines, ticks, and minor ticks from the non-orientationed axis.
Axis ticks on discrete axes are removed. Removes panel gridlines on the
orientationed axis only when at least one axis is discrete.
}
