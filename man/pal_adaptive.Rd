% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DEV.R
\name{pal_adaptive}
\alias{pal_adaptive}
\title{Generic Adaptive Palette Function with Optional Paletteer Support}
\usage{
pal_adaptive(palette, ..., rev = FALSE, interpolate = TRUE)
}
\arguments{
\item{palette}{Either:
\itemize{
\item A string in paletteer format: "package::palette" (requires paletteer)
\item A palette function that takes \code{n} as its first argument
\item A character vector of colors to interpolate
\item A single color name/code (will create gradient)
}}

\item{...}{Additional arguments passed to the palette function}

\item{rev}{Logical. If \code{TRUE}, reverses the behavior of the direction
adjustment based on panel background. Default is \code{FALSE}.}

\item{interpolate}{Logical. If \code{TRUE} and palette is a vector of colors,
interpolate between them. Default is \code{TRUE}.}
}
\value{
A function that takes a single argument \code{n} and returns a vector
of \code{n} colors
}
\description{
Returns a palette function that automatically adjusts direction based on
panel background luminance to maximize contrast. Works with any continuous
color palette. If paletteer is installed, also supports paletteer syntax.
}
\examples{
# With palette functions (no paletteer needed)
pal <- pal_adaptive(scales::viridis_pal())
pal(5)

# With color vectors (no paletteer needed)
pal <- pal_adaptive(c("#f0f0f0", "#0095A8FF"))

\dontrun{
# With paletteer syntax (requires paletteer package)
pal <- pal_adaptive("scico::berlin")
pal(5)
}
}
