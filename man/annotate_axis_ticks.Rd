% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate_axis_line.R, R/annotate_axis_ticks.R
\name{annotate_axis_ticks}
\alias{annotate_axis_ticks}
\title{Annotated axis ticks segments}
\usage{
annotate_axis_ticks(
  ...,
  x_breaks = NULL,
  y_breaks = NULL,
  x_position = "bottom",
  y_position = "left",
  colour = NULL,
  linewidth = NULL,
  length = NULL,
  theme_elements = "transparent"
)

annotate_axis_ticks(
  ...,
  x_breaks = NULL,
  y_breaks = NULL,
  x_position = "bottom",
  y_position = "left",
  colour = NULL,
  linewidth = NULL,
  length = NULL,
  theme_elements = "transparent"
)
}
\arguments{
\item{...}{Provided to require argument naming, support trailing commas etc.}

\item{x_breaks}{A vector of x-axis breaks for bottom/top axis ticks. If NULL, no x-axis ticks are drawn.}

\item{y_breaks}{A vector of y-axis breaks for left/right axis ticks. If NULL, no y-axis ticks are drawn.}

\item{x_position}{The position of the x-axis. One of "bottom" or "top". Defaults "bottom".}

\item{y_position}{The position of the y-axis. One of "left" or "right". Defaults "left".}

\item{colour}{The colour of the annotated segment. Inherits from the current theme axis.ticks etc.}

\item{linewidth}{The linewidth of the annotated segment. Inherits from the current theme axis.ticks etc.}

\item{length}{The absolute length of the annotated segment as a grid unit. Defaults to unit(11/3, "pt").}

\item{theme_elements}{What to do with theme axis tick elements. Either "transparent", "keep" or "blank". Defaults "transparent".}

\item{breaks}{A vector of breaks.}

\item{position}{The position of the axis. One of "bottom", "top", "left" or "right".}
}
\value{
A list of a annotate layer and theme elements.

A list of a annotate layer and theme elements.
}
\description{
Replace axis ticks with annotated segments using absolute measurements.
This function only works when panel dimensions are set uniformly via panel.widths and panel.heights.

Replace axis ticks with annotated segments using absolute measurements.
This function only works when panel dimensions are set uniformly via panel.widths and panel.heights.
It requires a \code{coord} of \code{ggplot2::coord_cartesian(clip = "off")}.
}
\examples{
library(ggplot2)
library(dplyr)
library(stringr)
library(palmerpenguins)

set_blanket(
  theme = theme_lighter(
    panel_heights = rep(unit(50, "mm"), 100),
    panel_widths = rep(unit(75, "mm"), 100),
  ),
)

penguins |>
  tidyr::drop_na(sex) |>
  mutate(across(sex, \(x) str_to_sentence(x))) |>
  gg_histogram(
    x = flipper_length_mm,
    col = sex,
  ) +
  annotate_axis_ticks(x_breaks = c(185, 195))

penguins |>
  tidyr::drop_na(sex) |>
  mutate(across(sex, \(x) str_to_sentence(x))) |>
  gg_blanket(
    x = flipper_length_mm,
    y = body_mass_g,
    col = sex,
  ) +
  annotate_axis_ticks(
    x_breaks = c(185, 195, 205, 215, 225),
    y_breaks = c(3500, 4500, 5500)
  ) +
  geom_point()

}
