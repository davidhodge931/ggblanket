% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate_axis_ticks.R
\name{annotate_axis_ticks}
\alias{annotate_axis_ticks}
\title{Annotated axis ticks segments}
\usage{
annotate_axis_ticks(
  axis,
  ...,
  breaks,
  position = NULL,
  colour = NULL,
  linewidth = NULL,
  length = NULL,
  theme_element = "transparent"
)
}
\arguments{
\item{axis}{The axis to annotate. One of "x" or "y".}

\item{...}{Provided to require argument naming, support trailing commas etc.}

\item{breaks}{A vector of axis breaks for axis ticks.}

\item{position}{The position of the axis ticks. For x-axis: "bottom" or "top". For y-axis: "left" or "right". Defaults to "bottom" for x-axis and "left" for y-axis.}

\item{colour}{The colour of the annotated segment. Inherits from the current theme axis.ticks etc.}

\item{linewidth}{The linewidth of the annotated segment. Inherits from the current theme axis.ticks etc.}

\item{length}{The absolute length of the annotated segment as a grid unit. Defaults to unit(11/3, "pt").}

\item{theme_element}{What to do with the equivalent theme element. Either "transparent", "keep" or "blank". Defaults "transparent".}
}
\value{
A list of a annotate layer and theme elements.
}
\description{
Replace axis ticks with annotated segments using absolute measurements.
This function only works when panel dimensions are set via panel.widths and panel.heights.
It requires a \code{coord} of \code{ggplot2::coord_cartesian(clip = "off")}.
}
\examples{
library(ggplot2)

set_blanket(
  theme = theme_lighter(
    panel_heights = rep(unit(50, "mm"), 100),
    panel_widths = rep(unit(75, "mm"), 100),
  ),
)

palmerpenguins::penguins |>
  gg_blanket(
    x = flipper_length_mm,
    y = body_mass_g,
    x_title = "Flipper length",
    y_title = "Body mass",
  ) +
  annotate_axis_ticks(
    axis = "y",
    breaks = seq(3000, 6000, 1000),
  ) +
  geom_point(
    colour = col_multiply(get_geom_defaults("point")$colour),
  )

}
