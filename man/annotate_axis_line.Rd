% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate_axis_line.R
\name{annotate_axis_line}
\alias{annotate_axis_line}
\title{Annotate axis line segment}
\usage{
annotate_axis_line(
  ...,
  position,
  colour = NULL,
  linewidth = NULL,
  theme_element = "transparent"
)
}
\arguments{
\item{...}{Arguments passed to \code{ggplot2::annotate("segment", ....)}. Require named arguments (and support trailing commas).}

\item{position}{The position of the axis line. One of "top", "bottom", "left", or "right".}

\item{colour}{The colour of the annotated segment. Inherits from the current theme axis.line etc.}

\item{linewidth}{The linewidth of the annotated segment. Inherits from the current theme axis.line etc.}

\item{theme_element}{What to do with the equivalent theme element. Either "transparent", "keep" or "blank". Defaults "transparent".}
}
\value{
A list of a annotate layer and theme elements.
}
\description{
Create an axis line with an annotated segment.

This function is designed to work with a theme that is globally set, so that the annotated line can be made consistent by default.

It should be used with a \code{coord} of \code{ggplot2::coord_cartesian(clip = "off")}.
}
\examples{
library(ggplot2)

set_blanket(
  theme = theme_lighter(
    panel_heights = rep(unit(50, "mm"), 100),
    panel_widths = rep(unit(75, "mm"), 100),
  ),
)

palmerpenguins::penguins |>
  tibble::add_row(flipper_length_mm = 195, body_mass_g = 2500, sex = "Female") |>
  gg_blanket(
    x = flipper_length_mm,
    y = body_mass_g,
  ) +
  annotate_axis_line(
    position = "bottom",
    arrow = arrow(angle = 15, length = unit(5, "pt"), type = "closed"),
  ) +
  annotate_axis_line(
    position = "left",
    arrow = arrow(angle = 15, length = unit(5, "pt"), type = "closed"),
  ) +
  geom_point(
    colour = col_multiply(get_geom_defaults("point")$colour),
    size = 3,
  )

}
