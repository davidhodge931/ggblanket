% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate_axis_line.R
\name{annotate_axis_line}
\alias{annotate_axis_line}
\title{Annotated axis line segment}
\usage{
annotate_axis_line(
  axis,
  ...,
  position = NULL,
  colour = NULL,
  linewidth = NULL,
  theme_elements = "transparent"
)
}
\arguments{
\item{axis}{The axis to annotate. One of "x" or "y".}

\item{...}{Provided to require argument naming, support trailing commas etc.}

\item{position}{The position of the axis. For x-axis: "bottom" or "top". For y-axis: "left" or "right". Defaults to "bottom" for x-axis and "left" for y-axis.}

\item{colour}{The colour of the annotated segment. Inherits from the current theme axis.line etc.}

\item{linewidth}{The linewidth of the annotated segment. Inherits from the current theme axis.line etc.}

\item{theme_elements}{What to do with theme axis line elements. Either "transparent", "keep" or "blank". Defaults "transparent".}
}
\value{
A list of a annotate layer and theme elements.
}
\description{
Replace axis line with an annotated segment.
}
\examples{
library(ggplot2)
library(dplyr)
library(stringr)
library(palmerpenguins)

set_blanket(
  theme = theme_lighter(
    panel_heights = rep(unit(50, "mm"), 100),
    panel_widths = rep(unit(75, "mm"), 100),
  ),
)

penguins |>
  tidyr::drop_na(sex) |>
  mutate(across(sex, \(x) str_to_sentence(x))) |>
  add_row(flipper_length_mm = 195, body_mass_g = 2500, sex = "Female") |>
  gg_blanket(
    x = flipper_length_mm,
    y = body_mass_g,
    col = sex,
  ) +
  annotate_axis_line(axis = "x") +
  annotate_axis_line(axis = "y") +
  geom_point()

}
